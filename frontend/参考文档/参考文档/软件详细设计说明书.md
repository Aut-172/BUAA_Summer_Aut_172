# 软件详细设计说明书（源码一致版）

## 1 引言

### 1.1 编写目的

本文档依据当前源码对生活服务平台进行详细设计说明，描述系统实际采用的技术结构、类与模块关系、关键对象协作流程、接口实现和测试追踪。原始详细设计说明书中与源码不符的内容，例如 Vue3、Spring Cloud Gateway、Nacos、Redis 缓存、钱包提现、真实地图导航、团购核销完整闭环等，均不作为当前实现依据。

### 1.2 源码基线

- 前端目录：`frontend/`
- 后端目录：`backend/`
- 脚本目录：`scripts/`
- 前端技术：Vite、React 18、React Router、Axios。
- 后端技术：Java 21、Spring Boot 3.4.4、MyBatis-Plus、MySQL、H2 测试库、JWT、BCrypt。
- 运行端口：后端 `8081`，前端 `5173`。

### 1.3 设计约束

1. 当前后端为 Spring Boot 单体应用，按包进行业务模块划分，不是 Spring Cloud 微服务。
2. Redis 依赖存在于构建文件中，但应用启动类排除了 Redis 自动配置，当前业务不依赖 Redis。
3. 购物车、订单、优惠券、消息、评价等核心数据均由数据库表持久化。
4. 支付为内部模拟支付，当前实现为校验通过后直接成功。
5. 文档中的图统一使用 PlantUML 语言描述；图片由 `scripts/render-plantuml.ps1` 渲染并插入在 PlantUML 代码块之后。

## 2 总体架构设计

### 2.1 运行视图

```text
浏览器
  -> Vite/React 前端
  -> /api 代理
  -> Spring Boot 后端
  -> MyBatis-Plus Mapper
  -> MySQL 数据库
```

前端统一通过 `frontend/src/utils/api.js` 调用 `/api` 前缀接口，Vite 开发代理把请求转发到 `http://localhost:8081`。后端通过 Controller、Service、Mapper 三层组织业务，统一响应体为 `Result` 或 `PageResult`。

### 2.2 包结构

| 包 | 职责 |
|---|---|
| `auth` | 用户、商家、骑手、管理员注册登录和身份实体。 |
| `common` | 统一响应、异常、JWT、拦截器、分页、日志切面。 |
| `user` | 消费者个人资料、地址、购物车。 |
| `merchant` | 公共商家浏览、商家资料、商品、规格。 |
| `order` | 订单创建、取消、完成、商家订单处理、订单明细。 |
| `payment` | 模拟支付和支付记录查询。 |
| `coupon` | 优惠券领取、锁定、释放、核销。 |
| `delivery` | 消费者配送追踪信息。 |
| `rider` | 骑手资料、任务池、接单与完成配送。 |
| `admin` | 管理员查看、审核、冻结/解冻主体和订单查询。 |
| `dashboard` | 按角色返回统计看板。 |
| `review` | 已完成订单评价和商家评分更新。 |
| `search` | 商家和商品关键词搜索。 |
| `recommend` | 基于评分、销量、距离的推荐。 |
| `message` | 文字消息、会话列表、未读数。 |

### 2.3 前端模块

| 文件 | 职责 |
|---|---|
| `src/main.jsx` | React 入口、路由、顶栏、受保护路由。 |
| `src/utils/ApiProvider.jsx` | 登录态、token 注入、全局通知、登出。 |
| `src/utils/api.js` | Axios 客户端和所有后端接口封装。 |
| `src/pages/Login.jsx` | 多角色登录/注册。 |
| `src/pages/Home.jsx` | 分类、商家列表、筛选。 |
| `src/pages/MerchantDetail.jsx` | 商家详情、商品规格、加入购物车。 |
| `src/pages/Cart.jsx` | 购物车分组、数量、删除、结算入口。 |
| `src/pages/Checkout.jsx` | 订单确认、地址、优惠券、下单并支付。 |
| `src/pages/Orders.jsx` | 订单列表、支付、取消、确认收货、支付记录。 |
| `src/pages/Delivery.jsx` | 配送追踪详情。 |
| `src/pages/Profile.jsx` | 用户资料与地址管理。 |
| `src/pages/MerchantConsole.jsx` | 商家看板、资料、商品、订单处理。 |
| `src/pages/RiderConsole.jsx` | 骑手看板、资料、任务流转。 |
| `src/pages/AdminConsole.jsx` | 管理员主体和订单管理。 |

## 3 类图

```plantuml
@startuml
skinparam classAttributeIconSize 0

class DemoApplication
class WebMvcConfig
class JwtAuthInterceptor
class JwtUtil
class Result
class PageResult
class BusinessException
class GlobalExceptionHandler
class LogAspect

class AuthController
class AuthService
class UserController
class UserService
class MerchantController
class MerchantService
class OrderController
class MerchantOrderController
class OrderService
class PaymentController
class PaymentService
class CouponController
class CouponService
class RiderController
class RiderService
class AdminController
class AdminService
class DashboardController
class DashboardService
class DeliveryController
class DeliveryService
class ReviewController
class ReviewService
class SearchController
class SearchService
class RecommendController
class RecommendService
class MessageController
class MessageService

class User {
  id: Long
  username: String
  phone: String
  nickname: String
  role: String
  status: String
}
class Admin {
  id: Long
  username: String
}
class Merchant {
  id: Long
  username: String
  name: String
  phone: String
  status: String
  rating: BigDecimal
  minDeliveryFee: BigDecimal
  deliveryFee: BigDecimal
}
class Rider {
  id: Long
  name: String
  phone: String
  status: String
  serviceArea: String
}
class Category
class Product {
  id: Long
  merchantId: Long
  categoryId: Long
  price: BigDecimal
  stock: Integer
  status: String
}
class SpecGroup
class ProductSpec
class Address
class Cart
class Orders {
  id: Long
  orderNo: String
  userId: Long
  merchantId: Long
  riderId: Long
  status: String
  actualAmount: BigDecimal
}
class OrderItem
class Payment
class Coupon
class UserCoupon
class Review
class Message

class UserMapper
class AdminMapper
class MerchantMapper
class RiderMapper
class CategoryMapper
class ProductMapper
class ProductSpecMapper
class SpecGroupMapper
class AddressMapper
class CartMapper
class OrdersMapper
class OrderItemMapper
class PaymentMapper
class CouponMapper
class UserCouponMapper
class ReviewMapper
class MessageMapper

DemoApplication --> WebMvcConfig
WebMvcConfig --> JwtAuthInterceptor
JwtAuthInterceptor --> JwtUtil
GlobalExceptionHandler --> Result

AuthController --> AuthService
UserController --> UserService
MerchantController --> MerchantService
OrderController --> OrderService
MerchantOrderController --> OrderService
PaymentController --> PaymentService
CouponController --> CouponService
RiderController --> RiderService
AdminController --> AdminService
DashboardController --> DashboardService
DeliveryController --> DeliveryService
ReviewController --> ReviewService
SearchController --> SearchService
RecommendController --> RecommendService
MessageController --> MessageService

AuthService --> UserMapper
AuthService --> MerchantMapper
AuthService --> RiderMapper
AuthService --> AdminMapper
AuthService --> JwtUtil
UserService --> UserMapper
UserService --> AddressMapper
UserService --> CartMapper
UserService --> ProductMapper
MerchantService --> MerchantMapper
MerchantService --> CategoryMapper
MerchantService --> ProductMapper
MerchantService --> SpecGroupMapper
MerchantService --> ProductSpecMapper
OrderService --> OrdersMapper
OrderService --> OrderItemMapper
OrderService --> ProductMapper
OrderService --> ProductSpecMapper
OrderService --> CartMapper
OrderService --> CouponService
PaymentService --> PaymentMapper
PaymentService --> OrdersMapper
CouponService --> CouponMapper
CouponService --> UserCouponMapper
RiderService --> OrdersMapper
RiderService --> RiderMapper
AdminService --> UserMapper
AdminService --> MerchantMapper
AdminService --> RiderMapper
AdminService --> OrdersMapper
ReviewService --> ReviewMapper
ReviewService --> OrdersMapper
MessageService --> MessageMapper

User "1" -- "0..*" Address
User "1" -- "0..*" Cart
User "1" -- "0..*" Orders
User "1" -- "0..*" UserCoupon
Merchant "1" -- "0..*" Product
Merchant "1" -- "0..*" Orders
Rider "1" -- "0..*" Orders
Category "1" -- "0..*" Product
Product "1" -- "0..*" SpecGroup
Product "1" -- "0..*" ProductSpec
Orders "1" -- "1..*" OrderItem
Orders "1" -- "0..*" Payment
Orders "1" -- "0..*" Review
Coupon "1" -- "0..*" UserCoupon
Orders "1" -- "0..*" Message
@enduml
```
<!-- plantuml-image: 01-3-类图 -->
![3 类图](软件详细设计说明书.assets/plantuml/01-3-类图.png)

## 4 核心设计说明

### 4.1 鉴权设计

登录成功后，`AuthService` 调用 `JwtUtil` 生成 JWT。前端 `ApiProvider` 把 token 保存到 localStorage，并通过 `api.js` 在请求头中附加 `Authorization: Bearer <token>`。后端 `JwtAuthInterceptor` 拦截 `/api/**`，放行认证、健康检查、商家浏览、商品详情、搜索、推荐等公共接口，并按路径前缀执行角色权限校验。

### 4.2 统一响应和异常

普通响应使用 `Result<T>`，分页响应使用 `PageResult<T>`。业务异常通过 `BusinessException` 抛出，`GlobalExceptionHandler` 将业务异常包装为统一 JSON。前端 `api.js` 对 `code != 200` 的响应抛出 `ApiError`。

### 4.3 订单与库存事务

`OrderService.checkout` 使用事务执行商家、商品、规格、库存、起送价和优惠券校验。订单采用下单减库存策略：创建订单前预扣 `product.stock` 和 `product_spec.stock`，取消订单时恢复库存。支付成功后订单进入 `pending_accept`，骑手接单或商家推进后进入 `delivering`，最终完成后进入 `completed`。

### 4.4 当前实现与设计预留

评价、搜索、推荐、消息已有后端 API，但前端入口并不完整。团购券核销、钱包提现、真实地图、Redis 缓存、超时自动取消未形成当前源码闭环，详细设计中仅作为后续扩展点记录。

## 5 用例对象级交互图

### UC01 多角色注册、登录与会话建立

```plantuml
@startuml
actor 用户
participant "loginPage:Login.jsx" as LoginPage
participant "session:ApiProvider" as Session
participant "api:api.js" as Api
participant "authController:AuthController" as AuthController
participant "authService:AuthService" as AuthService
participant "mapper:User/Merchant/Rider/AdminMapper" as Mapper
participant "jwt:JwtUtil" as Jwt

用户 -> LoginPage : 选择角色并提交凭证
LoginPage -> Session : login(role, username, password)
Session -> Api : auth.login(role, payload)
Api -> AuthController : POST /api/auth/.../login
AuthController -> AuthService : loginByRole(request)
AuthService -> Mapper : selectOne(username/phone)
Mapper --> AuthService : account
AuthService -> AuthService : BCrypt 校验密码和状态
alt 校验通过
  AuthService -> Jwt : generateToken(id, role, username)
  Jwt --> AuthService : accessToken
  AuthService --> AuthController : LoginResponse
  AuthController --> Api : Result.success
  Api --> Session : payload
  Session -> Session : persist localStorage + setAuthToken
  Session --> LoginPage : nextSession
else 校验失败
  AuthService --> AuthController : BusinessException
  AuthController --> Api : Result.error
  Api --> LoginPage : ApiError
end
@enduml
```
<!-- plantuml-image: 02-UC01-多角色注册、登录与会话建立 -->
![UC01 多角色注册、登录与会话建立](软件详细设计说明书.assets/plantuml/02-UC01-多角色注册、登录与会话建立.png)

### UC02 游客/用户发现商家与商品

```plantuml
@startuml
actor 访问者
participant "home:Home.jsx" as Home
participant "detail:MerchantDetail.jsx" as Detail
participant "api:api.js" as Api
participant "merchantController:MerchantController" as Controller
participant "merchantService:MerchantService" as Service
participant "merchantMapper:MerchantMapper" as MerchantMapper
participant "productMapper:ProductMapper" as ProductMapper
participant "categoryMapper:CategoryMapper" as CategoryMapper

访问者 -> Home : 打开首页/提交筛选
Home -> Api : getCategories(), getMerchants(params)
Api -> Controller : GET /api/categories
Controller -> Service : getAllCategories()
Service -> CategoryMapper : selectList(orderBy sortOrder)
CategoryMapper --> Service : categories
Api -> Controller : GET /api/merchants
Controller -> Service : getMerchantListWithProducts(keyword, category, page, size)
Service -> MerchantMapper : selectPage(active + filters)
MerchantMapper --> Service : merchantPage
loop 每个商家
  Service -> ProductMapper : select active products limit 3
  ProductMapper --> Service : products
end
Service --> Controller : Page<MerchantListDTO>
Controller --> Home : PageResult
访问者 -> Detail : 进入商家详情
Detail -> Api : getMerchant(id)
Api -> Controller : GET /api/merchants/{id}
Controller -> Service : getMerchantDetail(id)
Service -> ProductMapper : 查询商家 active 商品
Service -> CategoryMapper : 查询分类
Service --> Detail : MerchantProfileDTO
@enduml
```
<!-- plantuml-image: 03-UC02-游客-用户发现商家与商品 -->
![UC02 游客/用户发现商家与商品](软件详细设计说明书.assets/plantuml/03-UC02-游客-用户发现商家与商品.png)

### UC03 基于位置/评分/销量获得推荐商家

```plantuml
@startuml
actor 调用方
participant "recommendController:RecommendController" as Controller
participant "recommendService:RecommendService" as Service
participant "merchantMapper:MerchantMapper" as MerchantMapper
participant "productMapper:ProductMapper" as ProductMapper

调用方 -> Controller : GET /api/recommend?lat&lng
Controller -> Service : getRecommendations(lat, lng)
Service -> MerchantMapper : selectList(status=active)
MerchantMapper --> Service : merchants
loop 每个商家
  Service -> Service : calculateScore(rating, sales, distance)
  Service -> ProductMapper : select active products limit 3
  ProductMapper --> Service : products
  Service -> Service : buildRecommendVO
end
Service -> Service : 按得分降序排序
Service --> Controller : List<RecommendVO>
Controller --> 调用方 : Result.success
@enduml
```
<!-- plantuml-image: 04-UC03-基于位置-评分-销量获得推荐商家 -->
![UC03 基于位置/评分/销量获得推荐商家](软件详细设计说明书.assets/plantuml/04-UC03-基于位置-评分-销量获得推荐商家.png)

### UC04 消费者维护个人资料和收货地址

```plantuml
@startuml
actor 消费者
participant "profile:Profile.jsx" as Profile
participant "api:api.js" as Api
participant "jwtInterceptor:JwtAuthInterceptor" as Interceptor
participant "userController:UserController" as Controller
participant "userService:UserService" as Service
participant "userMapper:UserMapper" as UserMapper
participant "addressMapper:AddressMapper" as AddressMapper

消费者 -> Profile : 打开个人中心
Profile -> Api : getProfile(), getAddresses()
Api -> Interceptor : Authorization Bearer token
Interceptor -> Controller : request.userId
Controller -> Service : getProfile(userId), getAddressList(userId)
Service -> UserMapper : selectById(userId)
Service -> AddressMapper : selectList(userId)
Service --> Profile : 资料和地址列表
消费者 -> Profile : 提交资料或地址变更
Profile -> Api : updateProfile/addAddress/updateAddress/deleteAddress
Api -> Controller : /api/user/profile 或 /api/user/addresses
Controller -> Service : 执行业务方法(userId, body)
Service -> Service : 校验手机号唯一/地址归属/默认地址
Service -> UserMapper : updateById
Service -> AddressMapper : insert/update/delete
Service --> Profile : 最新数据
@enduml
```
<!-- plantuml-image: 05-UC04-消费者维护个人资料和收货地址 -->
![UC04 消费者维护个人资料和收货地址](软件详细设计说明书.assets/plantuml/05-UC04-消费者维护个人资料和收货地址.png)

### UC05 消费者管理购物车并形成结算清单

```plantuml
@startuml
actor 消费者
participant "detail:MerchantDetail.jsx" as Detail
participant "cartPage:Cart.jsx" as CartPage
participant "api:api.js" as Api
participant "userController:UserController" as Controller
participant "userService:UserService" as Service
participant "productMapper:ProductMapper" as ProductMapper
participant "cartMapper:CartMapper" as CartMapper

消费者 -> Detail : 点击加入购物车
Detail -> Api : user.addCart(merchantId, productId, quantity, specLabel)
Api -> Controller : POST /api/user/cart
Controller -> Service : addCart(userId, request)
Service -> ProductMapper : selectById(productId)
ProductMapper --> Service : product
Service -> CartMapper : 查询同商品同规格购物车项
alt 已存在
  Service -> CartMapper : update quantity
else 不存在
  Service -> CartMapper : insert cart snapshot
end
Service --> Detail : CartVO
消费者 -> CartPage : 调整数量/删除/清空
CartPage -> Api : updateCartQuantity/deleteCart/clearCart
Api -> Controller : PUT/DELETE /api/user/cart
Controller -> Service : 更新购物车
Service -> CartMapper : update/delete/selectList
Service --> CartPage : 最新购物车
@enduml
```
<!-- plantuml-image: 06-UC05-消费者管理购物车并形成结算清单 -->
![UC05 消费者管理购物车并形成结算清单](软件详细设计说明书.assets/plantuml/06-UC05-消费者管理购物车并形成结算清单.png)

### UC06 消费者领取并在订单中使用优惠券

```plantuml
@startuml
actor 消费者
participant "couponsPage:Coupons.jsx" as CouponsPage
participant "checkout:Checkout.jsx" as Checkout
participant "api:api.js" as Api
participant "couponController:CouponController" as Controller
participant "couponService:CouponService" as Service
participant "couponMapper:CouponMapper" as CouponMapper
participant "userCouponMapper:UserCouponMapper" as UserCouponMapper
participant "orderService:OrderService" as OrderService

消费者 -> CouponsPage : 领取优惠券
CouponsPage -> Api : coupons.claim(id)
Api -> Controller : POST /api/coupons/{id}/claim
Controller -> Service : claimCoupon(userId, couponId)
Service -> CouponMapper : selectById(couponId)
Service -> Service : 校验发布、有效期、总量、限领
Service -> UserCouponMapper : insert(status=unused)
Service -> CouponMapper : update claimedCount
Service --> CouponsPage : CouponVO
消费者 -> Checkout : 下单选择优惠券
Checkout -> Api : orders.checkout(couponId, items)
Api -> OrderService : POST /api/checkout
OrderService -> Service : lockCouponForOrder(userId, couponId, orderId, amount)
Service -> UserCouponMapper : 查询 unused 用户券
Service -> Service : 校验门槛
Service -> UserCouponMapper : update status=locked, orderId
alt 订单完成
  OrderService -> Service : confirmUseCoupon(orderId)
  Service -> UserCouponMapper : update status=used
else 订单取消
  OrderService -> Service : releaseCoupon(orderId)
  Service -> UserCouponMapper : update status=unused
end
@enduml
```
<!-- plantuml-image: 07-UC06-消费者领取并在订单中使用优惠券 -->
![UC06 消费者领取并在订单中使用优惠券](软件详细设计说明书.assets/plantuml/07-UC06-消费者领取并在订单中使用优惠券.png)

### UC07 消费者提交外卖订单

```plantuml
@startuml
actor 消费者
participant "checkout:Checkout.jsx" as Checkout
participant "api:api.js" as Api
participant "orderController:OrderController" as Controller
participant "orderService:OrderService" as Service
participant "merchantMapper:MerchantMapper" as MerchantMapper
participant "productMapper:ProductMapper" as ProductMapper
participant "productSpecMapper:ProductSpecMapper" as SpecMapper
participant "ordersMapper:OrdersMapper" as OrdersMapper
participant "orderItemMapper:OrderItemMapper" as ItemMapper
participant "cartMapper:CartMapper" as CartMapper
participant "couponService:CouponService" as CouponService

消费者 -> Checkout : 提交地址、商品、优惠券
Checkout -> Api : orders.checkout(payload)
Api -> Controller : POST /api/checkout
Controller -> Service : checkout(userId, request)
Service -> MerchantMapper : selectById(merchantId)
loop 每个商品
  Service -> ProductMapper : selectById(productId)
  Service -> SpecMapper : 按 productId + specLabel 查询规格
  Service -> Service : 校验状态、归属、库存、规格
end
Service -> Service : 计算商品总额、配送费、起送价
Service -> ProductMapper : 扣减商品库存
Service -> SpecMapper : 扣减规格库存
Service -> OrdersMapper : insert order(pending_payment)
loop 每个明细
  Service -> ItemMapper : insert order_item
end
opt 使用优惠券
  Service -> CouponService : lockCouponForOrder(...)
  Service -> OrdersMapper : update discount/actualAmount
end
Service -> CartMapper : delete 当前商家购物车
Service --> Checkout : OrderVO
@enduml
```
<!-- plantuml-image: 08-UC07-消费者提交外卖订单 -->
![UC07 消费者提交外卖订单](软件详细设计说明书.assets/plantuml/08-UC07-消费者提交外卖订单.png)

### UC08 消费者支付待支付订单

```plantuml
@startuml
actor 消费者
participant "ordersPage:Orders.jsx/Checkout.jsx" as Page
participant "api:api.js" as Api
participant "paymentController:PaymentController" as Controller
participant "paymentService:PaymentService" as Service
participant "ordersMapper:OrdersMapper" as OrdersMapper
participant "paymentMapper:PaymentMapper" as PaymentMapper

消费者 -> Page : 点击支付
Page -> Api : orders.pay(orderId, payMethod)
Api -> Controller : POST /api/orders/{id}/pay
Controller -> Service : pay(userId, orderId, payMethod)
Service -> OrdersMapper : selectOne(id + userId)
alt 状态为 pending_payment
  Service -> PaymentMapper : insert SUCCESS payment
  Service -> OrdersMapper : update status=pending_accept, paidAt
  Service --> Controller : PaymentVO
  Controller --> Page : 最新 OrderVO
else 状态不允许
  Service --> Controller : BusinessException
  Controller --> Page : Result.error
end
@enduml
```
<!-- plantuml-image: 09-UC08-消费者支付待支付订单 -->
![UC08 消费者支付待支付订单](软件详细设计说明书.assets/plantuml/09-UC08-消费者支付待支付订单.png)

### UC09 消费者取消未完成订单

```plantuml
@startuml
actor 消费者
participant "ordersPage:Orders.jsx" as Page
participant "api:api.js" as Api
participant "orderController:OrderController" as Controller
participant "orderService:OrderService" as Service
participant "ordersMapper:OrdersMapper" as OrdersMapper
participant "orderItemMapper:OrderItemMapper" as ItemMapper
participant "productMapper:ProductMapper" as ProductMapper
participant "productSpecMapper:ProductSpecMapper" as SpecMapper
participant "couponService:CouponService" as CouponService

消费者 -> Page : 取消订单
Page -> Api : orders.cancel(orderId)
Api -> Controller : POST /api/orders/{id}/cancel
Controller -> Service : cancelOrder(userId, orderId)
Service -> OrdersMapper : selectOne(id + userId)
alt pending_payment 或 pending_accept
  Service -> ItemMapper : selectList(orderId)
  loop 每个订单明细
    Service -> ProductMapper : 恢复商品库存
    Service -> SpecMapper : 恢复规格库存
  end
  Service -> OrdersMapper : update status=cancelled
  opt 有优惠券
    Service -> CouponService : releaseCoupon(orderId)
  end
  Service --> Page : OrderVO(cancelled)
else 不允许取消
  Service --> Page : BusinessException
end
@enduml
```
<!-- plantuml-image: 10-UC09-消费者取消未完成订单 -->
![UC09 消费者取消未完成订单](软件详细设计说明书.assets/plantuml/10-UC09-消费者取消未完成订单.png)

### UC10 消费者追踪配送并确认收货

```plantuml
@startuml
actor 消费者
participant "deliveryPage:Delivery.jsx" as DeliveryPage
participant "ordersPage:Orders.jsx" as OrdersPage
participant "api:api.js" as Api
participant "deliveryController:DeliveryController" as DeliveryController
participant "deliveryService:DeliveryService" as DeliveryService
participant "orderController:OrderController" as OrderController
participant "orderService:OrderService" as OrderService
participant "ordersMapper:OrdersMapper" as OrdersMapper
participant "riderMapper:RiderMapper" as RiderMapper
participant "merchantMapper:MerchantMapper" as MerchantMapper
participant "couponService:CouponService" as CouponService

消费者 -> DeliveryPage : 打开配送页
DeliveryPage -> Api : orders.getDelivery(orderId)
Api -> DeliveryController : GET /api/delivery/{id}
DeliveryController -> DeliveryService : getDeliveryInfo(userId, orderId)
DeliveryService -> OrdersMapper : selectById(orderId)
DeliveryService -> DeliveryService : 校验订单归属
DeliveryService -> RiderMapper : selectById(riderId)
DeliveryService -> MerchantMapper : selectById(merchantId)
DeliveryService --> DeliveryPage : DeliveryVO
消费者 -> OrdersPage : 确认收货
OrdersPage -> Api : orders.complete(orderId)
Api -> OrderController : POST /api/orders/{id}/complete
OrderController -> OrderService : completeOrder(userId, orderId)
OrderService -> OrdersMapper : selectOne(id + userId)
OrderService -> OrdersMapper : update status=completed, completedAt
OrderService -> CouponService : confirmUseCoupon(orderId)
OrderService --> OrdersPage : OrderVO(completed)
@enduml
```
<!-- plantuml-image: 11-UC10-消费者追踪配送并确认收货 -->
![UC10 消费者追踪配送并确认收货](软件详细设计说明书.assets/plantuml/11-UC10-消费者追踪配送并确认收货.png)

### UC11 消费者评价已完成订单

```plantuml
@startuml
actor 消费者
participant "apiCaller:Review UI/API" as Caller
participant "reviewController:ReviewController" as Controller
participant "reviewService:ReviewService" as Service
participant "ordersMapper:OrdersMapper" as OrdersMapper
participant "orderItemMapper:OrderItemMapper" as ItemMapper
participant "reviewMapper:ReviewMapper" as ReviewMapper
participant "merchantMapper:MerchantMapper" as MerchantMapper

消费者 -> Caller : 提交订单商品评价
Caller -> Controller : POST /api/reviews
Controller -> Service : submitReview(userId, request)
Service -> OrdersMapper : selectOne(orderId + userId)
Service -> ReviewMapper : countByOrderId
Service -> ItemMapper : selectList(orderId)
loop 每条商品评价
  Service -> Service : 校验商品归属、评分、内容长度
  Service -> ReviewMapper : insert review
  Service -> ItemMapper : update reviewed=true
end
Service -> ReviewMapper : select merchant reviews
Service -> Service : 计算平均评分
Service -> MerchantMapper : update merchant.rating
Service --> Caller : List<ReviewVO>
@enduml
```
<!-- plantuml-image: 12-UC11-消费者评价已完成订单 -->
![UC11 消费者评价已完成订单](软件详细设计说明书.assets/plantuml/12-UC11-消费者评价已完成订单.png)

### UC12 商家注册/启用并进入经营后台

```plantuml
@startuml
actor 商家
participant "loginPage:Login.jsx" as LoginPage
participant "session:ApiProvider" as Session
participant "api:api.js" as Api
participant "authController:AuthController" as Controller
participant "authService:AuthService" as Service
participant "merchantMapper:MerchantMapper" as MerchantMapper
participant "jwt:JwtUtil" as Jwt

商家 -> LoginPage : 选择商家注册
LoginPage -> Session : register(role=merchant,...)
Session -> Api : auth.register(merchant,payload)
Api -> Controller : POST /api/auth/merchant/register
Controller -> Service : registerMerchant(request)
Service -> MerchantMapper : 校验 username/phone 唯一
Service -> MerchantMapper : insert Merchant(status=active)
Service --> Session : 注册成功
Session -> Api : auth.login(merchant,payload)
Api -> Controller : POST /api/auth/merchant/login
Controller -> Service : loginMerchant(request)
Service -> MerchantMapper : selectOne(username)
Service -> Jwt : generateToken(merchantId,"merchant",username)
Service --> LoginPage : merchant session
@enduml
```
<!-- plantuml-image: 13-UC12-商家注册-启用并进入经营后台 -->
![UC12 商家注册/启用并进入经营后台](软件详细设计说明书.assets/plantuml/13-UC12-商家注册-启用并进入经营后台.png)

### UC13 商家维护店铺资料

```plantuml
@startuml
actor 商家
participant "merchantConsole:MerchantConsole.jsx" as Page
participant "api:api.js" as Api
participant "merchantController:MerchantController" as Controller
participant "merchantService:MerchantService" as Service
participant "merchantMapper:MerchantMapper" as MerchantMapper

商家 -> Page : 打开商家中心
Page -> Api : merchant.getProfile()
Api -> Controller : GET /api/merchant/profile
Controller -> Service : getMerchantBasicInfo(merchantId)
Service -> MerchantMapper : selectById(merchantId)
Service --> Page : Merchant
商家 -> Page : 提交店铺资料
Page -> Api : merchant.updateProfile(payload)
Api -> Controller : PUT /api/merchant/profile
Controller -> Controller : 清空 username/password
Controller -> Service : updateMerchantProfile(merchantId, merchant)
Service -> Service : 清空 status/rating/monthlySales
Service -> MerchantMapper : updateById
Service --> Page : 更新后 Merchant
@enduml
```
<!-- plantuml-image: 14-UC13-商家维护店铺资料 -->
![UC13 商家维护店铺资料](软件详细设计说明书.assets/plantuml/14-UC13-商家维护店铺资料.png)

### UC14 商家管理商品、规格与库存

```plantuml
@startuml
actor 商家
participant "merchantConsole:MerchantConsole.jsx" as Page
participant "api:api.js" as Api
participant "merchantController:MerchantController" as Controller
participant "merchantService:MerchantService" as Service
participant "productMapper:ProductMapper" as ProductMapper
participant "specGroupMapper:SpecGroupMapper" as GroupMapper
participant "productSpecMapper:ProductSpecMapper" as SpecMapper

商家 -> Page : 加载商品管理
Page -> Api : merchant.getProducts(), public.getCategories()
Api -> Controller : GET /api/merchant/products
Controller -> Service : getMerchantProducts(merchantId)
Service -> ProductMapper : selectList(merchantId)
Service --> Page : products
alt 新增商品
  Page -> Api : merchant.createProduct(payload)
  Api -> Controller : POST /api/merchant/products
  Controller -> Service : addProduct(merchantId, product)
  Service -> ProductMapper : insert product
else 编辑商品
  Page -> Api : merchant.updateProduct(payload)
  Api -> Controller : PUT /api/merchant/products
  Service -> ProductMapper : selectById + 校验归属
  Service -> ProductMapper : updateById
else 删除商品
  Page -> Api : merchant.deleteProduct(productId)
  Service -> ProductMapper : selectById + deleteById
else 规格维护
  Page -> Api : spec group/spec value API
  Service -> GroupMapper : insert/delete
  Service -> SpecMapper : insert/delete
end
@enduml
```
<!-- plantuml-image: 15-UC14-商家管理商品、规格与库存 -->
![UC14 商家管理商品、规格与库存](软件详细设计说明书.assets/plantuml/15-UC14-商家管理商品、规格与库存.png)

### UC15 商家处理本店订单

```plantuml
@startuml
actor 商家
participant "merchantConsole:MerchantConsole.jsx" as Page
participant "api:api.js" as Api
participant "merchantOrderController:MerchantOrderController" as Controller
participant "orderService:OrderService" as Service
participant "ordersMapper:OrdersMapper" as OrdersMapper
participant "orderItemMapper:OrderItemMapper" as ItemMapper
participant "couponService:CouponService" as CouponService

商家 -> Page : 查看本店订单
Page -> Api : merchant.getOrders()
Api -> Controller : GET /api/merchant/orders
Controller -> Service : getMerchantOrders(merchantId)
Service -> OrdersMapper : selectList(merchantId)
Service -> ItemMapper : select order items
Service --> Page : List<OrderVO>
商家 -> Page : 更新订单状态
Page -> Api : merchant.updateOrder(orderId, status)
Api -> Controller : PUT /api/merchant/orders/{id}
Controller -> Service : updateMerchantOrder(merchantId, orderId, request)
Service -> OrdersMapper : selectOne(id + merchantId)
Service -> Service : normalizeStatusCode + 校验状态迁移
alt 合法迁移
  Service -> OrdersMapper : update status
  opt 完成订单且有优惠券
    Service -> CouponService : confirmUseCoupon(orderId)
  end
  Service --> Page : OrderVO
else 非法迁移
  Service --> Page : BusinessException
end
@enduml
```
<!-- plantuml-image: 16-UC15-商家处理本店订单 -->
![UC15 商家处理本店订单](软件详细设计说明书.assets/plantuml/16-UC15-商家处理本店订单.png)

### UC16 骑手接单并完成配送

```plantuml
@startuml
actor 骑手
participant "riderConsole:RiderConsole.jsx" as Page
participant "api:api.js" as Api
participant "riderController:RiderController" as Controller
participant "riderService:RiderService" as Service
participant "ordersMapper:OrdersMapper" as OrdersMapper
participant "orderItemMapper:OrderItemMapper" as ItemMapper
participant "merchantMapper:MerchantMapper" as MerchantMapper
participant "couponService:CouponService" as CouponService

骑手 -> Page : 打开骑手工作台
Page -> Api : rider.getTasks()
Api -> Controller : GET /api/rider/tasks
Controller -> Service : getTasks(riderId)
Service -> OrdersMapper : 查询待接单/配送中/已完成
Service -> ItemMapper : 查询订单明细摘要
Service -> MerchantMapper : 查询取餐商家信息
Service --> Page : RiderTaskVO
骑手 -> Page : 接单或完成配送
Page -> Api : rider.updateTask(orderId, status)
Api -> Controller : PUT /api/rider/tasks/{id}
Controller -> Service : updateTask(riderId, orderId, request)
Service -> OrdersMapper : selectById(orderId)
Service -> Service : 校验状态和任务归属
alt 接单
  Service -> OrdersMapper : update riderId, status=delivering
else 完成配送
  Service -> OrdersMapper : update status=completed, completedAt
  Service -> CouponService : confirmUseCoupon(orderId)
end
Service --> Page : TaskItem
@enduml
```
<!-- plantuml-image: 17-UC16-骑手接单并完成配送 -->
![UC16 骑手接单并完成配送](软件详细设计说明书.assets/plantuml/17-UC16-骑手接单并完成配送.png)

### UC17 骑手维护个人资料

```plantuml
@startuml
actor 骑手
participant "riderConsole:RiderConsole.jsx" as Page
participant "api:api.js" as Api
participant "riderController:RiderController" as Controller
participant "riderService:RiderService" as Service
participant "riderMapper:RiderMapper" as RiderMapper

骑手 -> Page : 提交昵称/手机号/服务区域
Page -> Api : rider.updateProfile(profileForm)
Api -> Controller : PUT /api/rider/profile
Controller -> Service : updateProfile(riderId, request)
Service -> RiderMapper : selectById(riderId)
opt 修改手机号
  Service -> RiderMapper : select duplicate phone
end
alt 校验通过
  Service -> RiderMapper : updateById
  Service -> RiderMapper : selectById
  Service --> Page : Rider
else 手机号重复或骑手不存在
  Service --> Page : BusinessException
end
@enduml
```
<!-- plantuml-image: 18-UC17-骑手维护个人资料 -->
![UC17 骑手维护个人资料](软件详细设计说明书.assets/plantuml/18-UC17-骑手维护个人资料.png)

### UC18 管理员管理用户/商家/骑手状态

```plantuml
@startuml
actor 管理员
participant "adminConsole:AdminConsole.jsx" as Page
participant "api:api.js" as Api
participant "adminController:AdminController" as Controller
participant "adminService:AdminService" as Service
participant "userMapper:UserMapper" as UserMapper
participant "merchantMapper:MerchantMapper" as MerchantMapper
participant "riderMapper:RiderMapper" as RiderMapper

管理员 -> Page : 打开管理中心
Page -> Api : getUsers/getMerchants/getRiders
Api -> Controller : GET /api/admin/users|merchants|riders
Controller -> Service : listUsers/listMerchants/listRiders
Service -> UserMapper : selectPage
Service -> MerchantMapper : selectPage
Service -> RiderMapper : selectPage
Service --> Page : PageResult
管理员 -> Page : 审核/冻结/解冻
Page -> Api : audit/freeze/unfreeze
Api -> Controller : PUT 或 DELETE /api/admin/...
Controller -> Service : 对应管理方法
Service -> Service : selectById + 设置状态
Service -> UserMapper : updateById
Service -> MerchantMapper : updateById
Service -> RiderMapper : updateById
Service --> Page : 操作成功
@enduml
```
<!-- plantuml-image: 19-UC18-管理员管理用户-商家-骑手状态 -->
![UC18 管理员管理用户/商家/骑手状态](软件详细设计说明书.assets/plantuml/19-UC18-管理员管理用户-商家-骑手状态.png)

### UC19 管理员查看平台订单

```plantuml
@startuml
actor 管理员
participant "adminConsole:AdminConsole.jsx" as Page
participant "api:api.js" as Api
participant "adminController:AdminController" as Controller
participant "adminService:AdminService" as Service
participant "ordersMapper:OrdersMapper" as OrdersMapper

管理员 -> Page : 查看平台订单
Page -> Api : admin.getOrders(params)
Api -> Controller : GET /api/admin/orders
Controller -> Service : listOrders(page, pageSize, keyword, status, type)
Service -> OrdersMapper : selectPage(filters)
OrdersMapper --> Service : IPage<Orders>
Service --> Page : PageResult<Orders>
opt 查看详情
  Page -> Api : GET /api/admin/orders/{id}
  Api -> Controller : getOrderDetail(id)
  Controller -> Service : getOrderDetail(id)
  Service -> OrdersMapper : selectById(id)
  Service --> Page : Orders
end
@enduml
```
<!-- plantuml-image: 20-UC19-管理员查看平台订单 -->
![UC19 管理员查看平台订单](软件详细设计说明书.assets/plantuml/20-UC19-管理员查看平台订单.png)

### UC20 多角色查看运营看板

```plantuml
@startuml
actor 登录用户
participant "roleConsole:MerchantConsole/RiderConsole" as Page
participant "api:api.js" as Api
participant "dashboardController:DashboardController" as Controller
participant "dashboardService:DashboardService" as Service
participant "ordersMapper:OrdersMapper" as OrdersMapper

登录用户 -> Page : 进入角色中心
Page -> Api : dashboard.getMine()
Api -> Controller : GET /api/dashboard
Controller -> Controller : 读取 request.role/userId/merchantId/riderId
alt consumer
  Controller -> Service : getConsumerData(userId)
  Service -> OrdersMapper : 查询用户订单统计
else merchant
  Controller -> Service : getMerchantData(merchantId)
  Service -> OrdersMapper : 查询本店订单和收入
else rider
  Controller -> Service : getRiderData(riderId)
  Service -> OrdersMapper : 查询骑手任务和收入
end
Service --> Controller : DashboardVO 子数据
Controller --> Page : DashboardVO
@enduml
```
<!-- plantuml-image: 21-UC20-多角色查看运营看板 -->
![UC20 多角色查看运营看板](软件详细设计说明书.assets/plantuml/21-UC20-多角色查看运营看板.png)

### UC21 角色间订单会话沟通

```plantuml
@startuml
actor 发送方
actor 接收方
participant "messageController:MessageController" as Controller
participant "messageService:MessageService" as Service
participant "messageMapper:MessageMapper" as MessageMapper
participant "userMapper/User/Merchant/RiderMapper" as ActorMapper

发送方 -> Controller : POST /api/messages
Controller -> Controller : 从 JWT 映射 senderType
Controller -> Service : sendMessage(senderId, senderType, request)
Service -> MessageMapper : insert Message(isRead=false)
Service --> 发送方 : MessageVO
接收方 -> Controller : GET /api/messages?targetId&orderId
Controller -> Service : getMessages(userId, userType, targetId, orderId)
Service -> MessageMapper : 查询双方消息
Service -> MessageMapper : 将接收方未读消息 update isRead=true
Service --> 接收方 : List<MessageVO>
接收方 -> Controller : GET /api/messages/threads
Controller -> Service : getThreads(userId, userType)
Service -> MessageMapper : 查询相关消息
Service -> ActorMapper : 查询对方名称和头像
Service --> 接收方 : ThreadVO + unreadCount
@enduml
```
<!-- plantuml-image: 22-UC21-角色间订单会话沟通 -->
![UC21 角色间订单会话沟通](软件详细设计说明书.assets/plantuml/22-UC21-角色间订单会话沟通.png)

## 6 数据库表设计

当前数据库初始化脚本为 `backend/src/main/resources/db/init.sql`，测试库脚本为 `backend/src/test/resources/db/test-schema.sql`。核心表包括：`admin`、`user`、`merchant`、`rider`、`category`、`product`、`spec_group`、`product_spec`、`address`、`coupon`、`user_coupon`、`cart`、`orders`、`order_item`、`group_coupon`、`payment`、`review`、`message`。

## 7 接口设计

| 模块 | 接口入口 | 说明 |
|---|---|---|
| 认证 | `/api/auth/**` | 消费者、商家、骑手、管理员登录注册。 |
| 公共浏览 | `/api/merchants`、`/api/merchants/{id}`、`/api/products/{id}`、`/api/categories` | 商家和商品展示。 |
| 消费者 | `/api/user/**` | 资料、地址、购物车。 |
| 订单 | `/api/checkout`、`/api/orders/**` | 下单、订单列表、取消、完成。 |
| 支付 | `/api/orders/{id}/pay`、`/api/payments/{id}` | 模拟支付和支付记录。 |
| 优惠券 | `/api/coupons/**` | 可领券、我的券、领取。 |
| 配送 | `/api/delivery/{id}` | 配送追踪。 |
| 商家 | `/api/merchant/**` | 店铺、商品、规格和本店订单。 |
| 骑手 | `/api/rider/**` | 骑手资料和任务。 |
| 管理员 | `/api/admin/**` | 平台主体和订单管理。 |
| 评价 | `/api/reviews`、`/api/*/reviews` | 提交和查询评价。 |
| 搜索推荐 | `/api/search`、`/api/recommend` | 搜索和推荐。 |
| 消息 | `/api/messages/**` | 消息、线程、未读数。 |

## 8 测试设计

| 测试对象 | 当前测试 |
|---|---|
| 优惠券异常与生命周期 | `claimCouponRejectsEmptyInventoryAndNullLimit`、`releaseCouponClearsOrderBinding`、`riderCompletionMarksLockedCouponAsUsed` |
| 配送归属 | `deliveryInfoRequiresOrderOwner` |
| 用户-骑手完整主流程 | `fullConsumerRiderFlowWorksEndToEnd` |
| 商家订单状态 | `merchantApiAcceptsChineseStatusValue` |
| 库存预扣与取消恢复 | `checkoutReservesInventoryAndCancelRestoresIt` |
| 后端接口 E2E | `scripts/e2e-backend-test.ps1` |

建议补充：认证失败、管理员冻结/解冻、商家商品管理、评价重复提交、消息会话权限、搜索推荐排序等测试。

## 9 后续扩展点

1. 将评价、消息、搜索、推荐补齐前端入口和验收脚本。
2. 为管理员审核状态、消息接收方和商家规格归属增加更严格校验。
3. 如确需恢复原文档中的微服务方案，应拆分服务并新增 Gateway、Nacos、Redis 配置，不应仅在文档中声明。
4. 如需真实地图和钱包提现，应新增外部接口、数据表、权限规则和测试用例。

