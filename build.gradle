subprojects {
    configurations.configureEach {
        resolutionStrategy.dependencySubstitution {
            substitute module('com.example:microservice-common') using project(':common-lib')
        }
    }
}
